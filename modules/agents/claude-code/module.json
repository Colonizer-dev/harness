{
  "kind": "agent",
  "id": "claude-code",
  "name": "Claude Code",
  "description": "Anthropic's Claude Code agent via the Claude Agent SDK. Questions to the user are always multiple choice.",
  "protocol": "colonizer-runner/1",
  "entry": ["node", "runner.mjs"],
  "requires": {
    "binaries": ["claude"],
    "image": "glibc + node >= 20"
  },
  "secrets": [
    {
      "env": ["CLAUDE_CODE_OAUTH_TOKEN", "ANTHROPIC_API_KEY"],
      "hosts": ["api.anthropic.com"]
    }
  ],
  "settings": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "title": "Model",
        "description": "Claude model alias or ID. Empty uses Claude Code's default.",
        "default": "",
        "env": "COLONIZER_MODEL"
      },
      "effort": {
        "type": "string",
        "title": "Effort",
        "description": "Reasoning effort. Empty uses the model default.",
        "enum": ["", "low", "medium", "high", "xhigh", "max"],
        "default": "",
        "env": "COLONIZER_EFFORT"
      },
      "enforce_choices": {
        "type": "boolean",
        "title": "Enforce choice cards",
        "description": "If the agent ends a turn with a plain-text question, ask it once to re-ask with choices.",
        "default": true,
        "env": "COLONIZER_ENFORCE_CHOICES"
      }
    }
  }
}
